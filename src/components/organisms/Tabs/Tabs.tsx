import { ReactNode } from 'react';

interface TabProps {
  title: string;
  icon: ReactNode;
  children: ReactNode;
}

export interface TabsProps {
  items: TabProps[];
}

export const Tabs = () => {
  <div></div>
};
